#include <stdio.h>
#include <stdlib.h>

int main()
{
    int n,a[100000],i,j;
    scanf("%d",&n);
    for(i=0;i<n;i++)
        scanf("%d",&a[i]);
    i--;
    for(j=i;j>=0;j--)
        printf("%d ",a[j]);
    return 0;
}

